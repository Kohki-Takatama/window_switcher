{
  "manifest_version": 3,
  "name": "Move Tab to Window",
  "version": "1.0",
  "description": "Use keyboard shortcuts to move and manage tabs and windows.",
  "permissions": [
    "tabs"
  ],
  "background": {
    "service_worker": "background.js"
  },
  "commands": {
    "move_tab_to_new_window": {
      "suggested_key": {
        "default": "MacCtrl+Down"
      },
      "description": "Move the current tab to a new window"
    },
    "move_tab_to_other_window": {
      "suggested_key": {
        "default": "MacCtrl+Up"
      },
      "description": "Move the current tab to another existing window"
    },
    "focus_next_tab": {
      "suggested_key": {
        "default": "MacCtrl+Right"
      },
      "description": "Move focus to the next tab"
    },
    "focus_next_window": {
      "suggested_key": {
        "default": "MacCtrl+Left"
      },
      "description": "Move focus to the next window"
    }
  }
}
